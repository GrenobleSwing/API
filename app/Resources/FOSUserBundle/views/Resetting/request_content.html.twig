{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_resetting_send_email')|short_path }}" method="POST"  ng-submit="processForm($event, 'POST', '{{ path('fos_user_resetting_send_email')|short_path }}')" class="fos_user_resetting_request">
    <div class="form-group">
        <label class="control-label required" for="username">{{ 'resetting.request.username'|trans }}</label>
        <input type="text" id="username" name="username" required="required" />
    </div>
    <div>
        <input class="btn-default btn" type="submit" value="{{ 'resetting.request.submit'|trans }}" />
    </div>
</form>
